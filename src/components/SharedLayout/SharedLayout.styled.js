import styled from 'styled-components';

export const LayoutHeader = styled.header`
  border-bottom: 1px solid rgba(79, 79, 111, 0.15);
  /* position: fixed;
  top: 0;
  left: 0;
  right: 0;
  background-color: white;
  z-index: 20; */
`;

export const Main = styled.main`
  display: flex;
  flex-grow: 1;
`;
